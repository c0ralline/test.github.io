# 使用python处理excel表格

步骤包括：

- 导入库
- 载入表格
- 按行操作

```python

import openpyxl

# 载入已有的表格
input_file_path = "path/test.xlsx"
sheet_name = "Sheet1"
input_file = openpyxl.load_workbook(input_file_path)
input_sheet = input_file[sheet_name]

# 按行操作
# 逐行遍历 Excel 工作表，同时为每一行添加一个计数器
# 返回形式为 (计数器值, 行数据) 的元组
for cnt,row in enumerate(input_sheet.iter_rows(),start=1):
  todo

# 创建新表格
output_file = openpyxl.Workbook()
sheet_name  = "Sheet2"
output_sheet = output_file.active
output_sheet.title = sheet_name

# 写表格
# 方法1
output_sheet['A1'] = "name"
# 方法2
output_sheet.cell(row=1,column=1).value = "name"

#保存文件
output_path = "path/output_file.xlsx"
output_file.save(output_path)
```
